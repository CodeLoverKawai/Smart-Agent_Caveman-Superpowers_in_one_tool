# smart-agent

## Build and Test Command
- Test command: `node --test tests/*.test.js`

## Coding Style & Rules
- Language/Runtime: Clean Node.js vanilla JS
- Style: Keep skills highly compressed (token-efficient), use CJS (CommonJS) in hooks for backward compatibility.
- Commits: Use short conventional commits.
